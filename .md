### Steps

## dotenv
```
pip install python-dotenv
create folder .env

# in settings.py
import os
from dotenv import load_dotenv
load_dotenv(BASE_DIR/'.env')

Example : 'PORT': os.getenv('DB_PORT'),
 
```

## postgress
```
pip install psycopg

#in .env
DB_NAME=instagram_db_mucb
DB_USER=instagram_db_mucb_user
DB_PASSWORD=wqLGLo00juMD6WiRX29bURVYIKacB2Fk
DB_HOST=dpg-d1nvlivfte5s739q5gpg-a.oregon-postgres.render.com
DB_PORT=5432

# in settings.py

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.postgresql',
        'NAME': os.getenv('DB_NAME'),
        'USER': os.getenv('DB_USER'),
        'PASSWORD': os.getenv('DB_PASSWORD'),
        'HOST': os.getenv('DB_HOST'),
        'PORT': os.getenv('DB_PORT'),
    }
}



```


## Media 
```
MEDIA_URL = '/media/'
MEDIA_ROOT = BASE_DIR / 'media'
```